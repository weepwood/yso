{
  "name": "yso-webhook-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test src/index.test.ts"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
